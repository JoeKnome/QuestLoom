# Item form should use shared location component

## Description

The item form should use the same location component as other entities(LocationPlacesEditor) to manage the locations of the item, rather than a separate internal component.
