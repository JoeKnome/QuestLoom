/**
 * Entity types that support discovery state (person, place, map).
 * Used by EntityDiscovery for spoiler-friendly hiding.
 */
export enum DiscoveryEntityType {
  PERSON = 0,
  PLACE = 1,
  MAP = 2,
}
